nclude <stdio.h>
#include <string.h>

/**
Jacqueline Failla

I've recieved compiler errors for my files
While the code should work fine its throwing randoms errors that I cant
find solutions for
so code is otherwise incomplete
**/

int main(){
  
  /*FILE *word; //Declares input txt FILE
  word = fopen("input.txt","r"); //opens input text file
  char *userchars; //Declare txt 
  
  FILE *output; //Declares output text FILE
  output = fopen("output.txt","a"); //opens output text FILE
  
  while(fgets(userchars,100,word)){ // Creates while loop for each line of text until null line
    int i; // declares counter for for loop
    for(i=0;i<strlen(userchars);i++){ // for each character in user char output double to file
      fprintf(output,"%c", userchars[i]); //output
      fprintf(output,"%c", userchars[i]);      
    }
  }
  fclose(word);
  fclose(output);*/
	printf('Hello World');
  return 0;
}
