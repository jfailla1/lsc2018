#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <vector>

using namespace std;

int solution (vector<int> &A) {
	int sol;
	
	return sol;
}
int main () {
	vector<int> *A;
	A = [1,3,5];
	for (int i = 0; i < 3; i++)
		cout << A[i] << endl;
	return 0;
}
